[project]
name = "fleet-copilot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "reiteristvan", email = "reiteristvan@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
fleet-copilot = "fleet_copilot:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
