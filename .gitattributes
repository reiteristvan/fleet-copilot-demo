# Normalise line endings so Windows and CI agree on file contents.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
# A .docx is a zip. text=auto would classify it by content heuristic, and the
# generated corpus commits its SHA-256, so the bytes must survive untouched.
*.docx binary
